import { createContext } from "react";

const ListDisplayContext = createContext();
const ListDisplayProvider = ListDisplayContext.Provider;

export { ListDisplayContext };
export default ListDisplayProvider;
