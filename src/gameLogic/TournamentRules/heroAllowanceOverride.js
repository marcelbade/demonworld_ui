

/**
 * tournaments will frequently override the percentage of heroes na dmages an army list can contain. The percentagess given for the different factions are then overridden by a single value.  
 */

let TOURNAMENT_VALUE_HEROES = 35;


/**
 * Most tournaments restrict lists to a maximum of two of every non-unique units, but this can be changes.
 */
let SAME_UNIT_SELECTION = 2; 
